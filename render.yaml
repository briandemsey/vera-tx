services:
  - type: web
    name: vera-tx
    runtime: python
    buildCommand: pip install -r requirements.txt && python teks_fetcher.py sync
    startCommand: streamlit run app.py --server.port=$PORT --server.address=0.0.0.0
